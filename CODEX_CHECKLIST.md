# Codex Workflow Checklist (Template Standard)

Use this every time you build or change anything with Codex.

---

## A) Start a new project (from the template)

```bash
cd ~/code/projects
gh repo create <my-app> --template byronraco/codex-webapp-template --private --clone
cd <my-app>
npm install
npm run dev
```
